Meeting to Merge preserves the path from a spoken requirement to a proposed code change. The fixture contains four timestamped requirements and an explicit definition of approval. Running the analysis maps each requirement to a baseline failing test, then produces a minimal unified diff covering input validation, approval, receipt identity, and idempotency. The patch stays behind a human apply boundary. Removing the approval definition makes the workflow stop for clarification instead of inventing product behavior.

